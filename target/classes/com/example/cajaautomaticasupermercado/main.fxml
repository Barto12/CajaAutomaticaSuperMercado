<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1">
    <top>
        <VBox spacing="10">
            <ImageView fx:id="logoImageView" fitHeight="100" preserveRatio="true" />
        </VBox>
    </top>
    <center>
        <VBox spacing="10" padding="10">
            <TextField fx:id="barcodeTextField" promptText="Código de Barras" />
            <TextField fx:id="quantityTextField" promptText="Cantidad" />
            <Button fx:id="addButton" text="Agregar" />
            <ListView fx:id="cartListView" />
        </VBox>
    </center>
    <bottom>
        <HBox spacing="10" padding="10">
            <Label fx:id="totalLabel" text="Total: $0.00" />
            <Button fx:id="payButton" text="Pagar" />
        </HBox>
    </bottom>
</BorderPane>
